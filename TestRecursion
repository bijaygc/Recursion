public class TestRecursion {
    public static void main(String[] args){
        int result=sum(1,10);
        System.out.println(result+" is total numbers:");
    }
   public static int sum(int a ,int b ){
if(b>=a){       
    return b+ sum(a,b-1);
    }else{
    return b;
}
   }
}
